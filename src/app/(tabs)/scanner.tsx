import { SectionPlaceholder } from '@/components/SectionPlaceholder';

export default function ScannerScreen() {
  return (
    <SectionPlaceholder
      title="Escáner"
      description="Escanea el código de barras de un producto para localizarlo."
      availability="El escáner se habilitará próximamente."
    />
  );
}
